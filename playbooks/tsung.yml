---
# Tsung controller/injector
#
#
# roles/tsung/vars/nodes.yml is generated by ec2tool.py
#
- hosts: controller
  sudo: True
  gather_facts: False
  vars_files:
    - roles/tsung/vars/nodes.yml
  roles:
    - resolv
    - tsung
    - tsung_scenario

- hosts: injectors
  sudo: True
  gather_facts: False
  vars_files:
    - roles/tsung/vars/nodes.yml
  roles:
    - resolv
    - tsung
